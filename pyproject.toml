[project]
name = "fire"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "alexxbez", email = "alexxbez@proton.me" }
]
requires-python = ">=3.14"
dependencies = [
    "mesa>=3.5.1",
    "networkx>=3.6.1",
]

[project.scripts]
fire = "fire:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
